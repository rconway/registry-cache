# Registry domain for virtual hosts
# e.g., registry.example.com for endpoints like docker.io.registry.example.com:5000
REGISTRY_DOMAIN=registry.example.com

# Docker Hub credentials for upstream access
DOCKERHUB_USERNAME=
DOCKERHUB_PASSWORD=

# GitHub Container Registry credentials for upstream access
GHCR_USERNAME=
GHCR_PASSWORD=

# Quay credentials for upstream access
QUAY_USERNAME=
QUAY_PASSWORD=
